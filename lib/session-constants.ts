// Demo-only session flag — there is no real auth backend yet.
// Login/register set this cookie purely so the marketing nav can
// demonstrate the logged-in state. Replace with real auth before launch.
export const SESSION_COOKIE = "ledgerly_demo_session";
